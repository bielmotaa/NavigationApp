{
  "compilerOptions": {
    "strict": true, // para que certos erros sejam tratados,  existe componentes que sem isso ele dá erro
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "extends": "expo/tsconfig.base"
}
